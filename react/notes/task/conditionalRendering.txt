
 Build a "User Greeting Card"
 Create a React component that displays different greetings based on user data.
 Requirements :
  Display Logic:
 If no user → Show "Please log in"

 If user exists:
 Show "Hi username"
 If user is premium → Show a gold star ⭐ badge
 If user has unread messages → Show message count in red
 If user's account is new (< 7 days) → Show "New Member" badge
 
  {
     name: "test",
     isPremium: true,
    unreadMessages: 5,
    accountCreatedDays: 3
}